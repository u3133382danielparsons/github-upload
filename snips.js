
// for (let j = 0; j <= 1; j++) {
                //     

                //     if (j == 0) {
                //         liThree.appendChild(document.createTextNode(`Life Expectancy is ${le} years.`));
                //     } else {
                //         liThree.appendChild(document.createTextNode(`Remaining Life Expectancy is ${rle} years.`));
                //     }
                // }
        // console.log(c3);
        // console.log(y3);
        // console.log(today);
        // console.log(m3);
        // console.log(a3);
        // console.log(g3);

// let females = data[i].females;
// let males = data[i].males;
// let total = data[i].females + data[i].males;
// for (let j = 0; j <= 2; j++) {
//     let li = document.createElement("li");
//     ul.appendChild(li);
//     li.classList.add("list-group-item");

//     if (j == 0) {
//         li.appendChild(document.createTextNode("Females: " + females));
//     } else if (j == 1) {
//         li.appendChild(document.createTextNode("Males: " + males));
//     } else {
//         li.appendChild(document.createTextNode("Total: " + total));
//     }

// }

    // SNIPS

        // let females = data[i].females;
        // let males = data[i].males;
        // let total = data[i].females + data[i].males;
        // console.log(data[i].age);
        // Create list items to display the population data


        // li - fm.classList.add("list-group-item");
        // li - fm.appendChild(document.createTextNode("females: " + females));
        // li.appendChild(document.createTextNode(males));

        // li - fm.classList.add("list-group-item");
        // li - fm.appendChild(document.createTextNode("females: " + females));

        // li.appendChild(document.createTextNode(total));

        // li.appendChild(document.createTextNode(total));


            // let a = document.createElement('a');
            // document.getElementsByClassName("list-group")[0].appendChild(a);

            // a.setAttribute('href', data[i].html_url);
